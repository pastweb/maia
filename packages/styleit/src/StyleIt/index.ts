import { add, replace, remove, getStyleItCacheTagString } from './styleIt';

export type { ScopedNames } from './addScopedCSS';

export default {
  add,
  replace,
  remove,
  getStyleItCacheTagString,
};
