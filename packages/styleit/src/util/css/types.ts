export type StyleInfo = {
  fileName?: string;
  componentName?: string;
  styleKey?: string; 
};

export type StyleDetail = {
  rules: string;
  fileName: string;
  componentName: string;
  styleKey: string;
};
